# Repo Authority Cleanup - CONFLICT REGISTER

| path | current_role | formal_caller | tests_tools_docs_caller | classification | why | delete_precondition | rollback_path |
|---|---|---|---|---|---|---|---|
| `identity dual-layer wording` (`openemotion.proto_self.state.IdentityInvariants` vs `openemotion.identity.*`) | resolved runtime authority, monitor docs/tools | formal mainline = `proto_self` identity only | old identity tools/docs still mention named owner | `reference_only` | code baseline is already single-authority, but historical docs/tools can still suggest the named owner is live | future doc/tool cleanup must remove remaining live-owner wording | revert identity reference-only constants/tests if needed |
| `self-model formal owner + active substrate + legacy adapter/mirror` | authority ambiguity resolved; delete admission still open | formal mainline = owner package + substrate compute layer | legacy daemon/tools/docs still reference adapter/mirror | `compat` | owner is formal authority, substrate remains active compute layer, and code-level tests/gate now prove legacy adapter/mirror are non-mainline surfaces | remaining work is only tool/archive cleanup before delete admission | restore adapter/mirror docs/constants/tests |
| `drives formal owner + proto_self appraisal substrate` | classified, unresolved authority consolidation | formal mainline = `_inject_endogenous_drive_context` + `endogenous_drive_context.py` | MVP14 tools/docs and substrate tests | `active_substrate` | authority and substrate callers are now explicit, but dual-layer semantics remain | dedicated drives wave | keep current owner/substrate split |
| `reflection formal owner + proto_self reflection trigger + legacy emotiond reflection surfaces` | classified, unresolved authority consolidation | formal mainline = `_inject_reflective_self_context` + `reflective_self_context.py` | `emotiond/core.py`, MVP15 tests/tools/docs | `reference_only` | formal owner is clear, but trigger substrate and legacy daemon surfaces still coexist outside formal mainline | dedicated reflection wave | keep current owner/substrate split |
| `developmental formal owner + proto_self_v2 surface + emotiond.developmental_core implementation library` | classified, unresolved authority consolidation | formal mainline = `_apply_developmental_self_writeback` + `developmental_self_context.py` + `proto_self_v2/developmental.py` | MVP12/16 tests/tools/docs | `active_substrate` | split stack is real and not safe to simplify in this wave | dedicated developmental identity task | keep current implementation library |
| `proto_self_restore delete admission` | code caller cleared; docs/generated residue remain | formal caller = 0 | historical docs/generated maps | `delete_candidate` | package re-export is removed, but generated/doc surfaces still mention the shim | archive or regenerate docs/maps, then re-run scoped tests | restore shim and re-export |
| `self_model_adapter delete admission` | unresolved candidate deletion | formal caller = 0 | legacy daemon/tools/docs | `delete_candidate` | no formal caller, but current legacy tool surface still exists | archive/migrate legacy daemon/tool callers first | restore adapter module |
| `self_model_mirror delete admission` | unresolved candidate deletion | formal caller = 0 | mirror tool/docs callers | `delete_candidate` | no formal caller, but mirror/report tooling still exists | archive/migrate mirror tooling first | restore mirror module |
| `historical artifact directories` (`artifacts/P*`, old eval/memory loops) | archive boundary unclear | no formal caller | docs/history only | `archive` | clearly non-current, but current archive layout does not yet exist | build archive/current index and path migration plan | keep in place or restore from archive |
