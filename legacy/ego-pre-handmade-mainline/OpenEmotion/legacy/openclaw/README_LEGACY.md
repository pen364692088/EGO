# Legacy OpenClaw Integration

> **仅历史兼容，不是正式主链**

此目录包含 OpenEmotion 早期作为 OpenClaw skill 的集成代码。

## 状态

- **类型**: Legacy / Compatibility Shim
- **正式主链**: `openemotion/` + `emotiond/` + EgoCore adapter
- **退出计划**: 当 `cycle_core_v1` + `result_v1` 正式接主链后移除

## 不再维护

此目录代码不再主动开发。新功能请在正式主链实现。
