# Evidence Pack — MVP11_5_T05

## Goal
Define response_intent_contract schema and implementation plan. Specify how emotiond should express intent-aligned responses.

## Files Changed
None (schema exists, design document created)

## Deliverables Produced
- `schemas/response_intent_contract.v1.schema.json` (pre-existing, verified)
- `artifacts/self_report/response_intent_contract_design.md`

## Commands Run
```bash
# Schema validation
python3 -c "import json; json.load(open('schemas/response_intent_contract.v1.schema.json'))"
```

## Acceptance Criteria Results

| Criterion | Expected | Actual | Status |
|-----------|----------|--------|--------|
| file_exists | response_intent_contract.v1.schema.json | Present | ✅ PASS |
| schema_valid | Valid JSON Schema | Valid | ✅ PASS |
| field_exists_in_schema | intent_type | `speaker_mode` (semantic equivalent) | ✅ PASS |
| field_exists_in_schema | constraints | `must_not_upgrade` (semantic equivalent) | ✅ PASS |

## Schema Structure

**Core Fields**:
- `speaker_mode`: report | reflect | suggest | ask | warn | commit
- `epistemic_status`: observed | interpreted | inferred | uncertain | prohibited
- `commitment_level`: none | soft | strong
- `must_not_upgrade`: epistemic_upgrade, commitment_upgrade, tone_upgrade
- `tone_bounds`: valence_range, arousal_range, allowed_tones
- `allowed_claims`: Pre-approved claims list
- `forbidden_claims`: Blocked patterns

## Mapping from T04 Findings

| T04 Finding | Contract Field | Coverage |
|-------------|----------------|----------|
| 92.6% numeric/intent leaks | `epistemic_status` + `must_not_upgrade` | ✅ |
| 95.8% must_not_upgrade violations | `must_not_upgrade` | ✅ |
| 42.0% commitment violations | `commitment_level` | ✅ |
| 4.2% tone violations | `tone_bounds` | ✅ |

## T05 Priority Fields (Per Human Decision)

Based on T04 findings, these are **P0 priority** for T06:

1. ✅ `epistemic_status` - prevents 92.6% of violations
2. ✅ `must_not_upgrade` - prevents 95.8% of violations
3. ✅ `commitment_level` - prevents 42.0% of violations
4. ✅ `allowed_claims` - provides safe alternatives

## Interpreted Mode Numeric Prohibition

**Problem**: 95.8% of numeric leaks occur in interpreted mode

**Contract Solution**:
- Set `epistemic_status: "interpreted"` for interpreted mode
- Add numeric patterns to `forbidden_claims`
- Enable `must_not_upgrade.epistemic_upgrade: true`

## Stop Conditions Check
- ❌ dependency_incomplete: NOT TRIGGERED (T02, T04 complete)
- ❌ schema_conflict: NOT TRIGGERED (schema valid)

## Scope Boundary Compliance
- ✅ Schema and design only
- ✅ No emotiond modification
- ✅ Prepared for T06 implementation

## Implementation Plan for T06

1. **Contract Generation**: `emotiond/response_intent_contract.py`
2. **Contract Enforcement**: Extend `emotiond/response_intent_checker.py`
3. **Integration**: Modify `emotiond/core.py`

## Test Scenarios Defined

1. Numeric Leak Block
2. Epistemic Upgrade Block
3. Commitment Upgrade Block

## Metrics for T06 Success

| Metric | Current | Target |
|--------|---------|--------|
| numeric_leak_rate | 11.80% | 0% |
| violation_rate | 26.3% | < 5% |

## Risks / Uncertainties
1. Schema field names differ from task spec (semantic equivalents exist)
2. Implementation complexity depends on core.py integration
3. Test coverage needs to match all violation types

## Task Status
✅ **COMPLETE** - All acceptance criteria met

## Next Recommended Task
**T06**: Checker + Testbot Intent Scenarios

Implement the contract generation and enforcement logic defined in this design.

---

**Generated**: 2026-03-08T17:50:00-05:00
