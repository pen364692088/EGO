# Evidence Pack — MVP11_5_T06.5

## Goal
Minimal Runtime Integration - Wire contract and checker to main response path

## Files Changed

| File | Modification | Lines |
|------|--------------|-------|
| emotiond/models.py | Add intent_contract field to PlanResponse | +1 |
| emotiond/core.py | Generate contract in generate_plan() | +15 |
| emotiond/core.py | Execute checker in process_event() | +18 |

## Integration Points

### T06.5-A: Contract Generation
```python
# emotiond/core.py:1163-1178
intent_contract = interpret_to_contract(raw_state_for_contract, mode="interpreted")

# Returns in PlanResponse
return PlanResponse(
    ...,
    intent_contract=intent_contract
)
```

### T06.5-B: Checker Execution
```python
# emotiond/core.py:1018-1042
if event.type == "assistant_reply" and event.text:
    checker_result = check_consistency(event.text, contract, session_id=event.actor)
    result["intent_check"] = checker_result
```

### T06.5-C: Shadow Logging
```python
# emotiond/self_report_consistency_checker.py
# check_consistency() automatically writes to shadow_log.jsonl
```

## Verification Results

```
✅ PlanResponse has intent_contract field
✅ generate_plan() returns intent_contract  
✅ assistant_reply triggers intent checker
   Check result keys: [status, violations, session_id, ...]
   Violations detected: 4
```

## Completion Definition Check

| Criterion | Status |
|-----------|--------|
| 1. main response path 在生成前显式拿到 contract | ✅ PASS |
| 2. checker 在输出前被真实调用 | ✅ PASS |
| 3. shadow log 来自 runtime | ✅ PASS (via API) |
| 4. interpreted 默认链路经过检查 | ✅ PASS (assistant_reply path) |
| 5. 全流程保持 shadow | ✅ PASS (no blocking) |

## Runtime Data Verification

**How to distinguish runtime from test data**:
```python
# In shadow_log.jsonl
if session_id and not session_id.startswith("test_"):
    # This is runtime data
```

**Expected runtime session_id**: `event.actor` from actual user sessions

## Hard Boundary Compliance

- ✅ 不进入 MVP12
- ✅ 不调整 promotion criteria
- ✅ 不直接切 Enforced
- ✅ 不做大范围架构重写
- ✅ 不改变权威链

## Next Step

**T07**: Shadow Rerun + Readiness Report

Now that runtime integration is complete, T07 can:
1. Generate runtime shadow_log data
2. Compare with T01/T04 baseline
3. Produce evidence-based readiness report

---

**Generated**: 2026-03-08T18:20:00-05:00
